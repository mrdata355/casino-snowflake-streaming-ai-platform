[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [".venv", "dbt/target", "dbt/dbt_packages"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests", "spark/tests"]
addopts = "--strict-markers --disable-warnings"
